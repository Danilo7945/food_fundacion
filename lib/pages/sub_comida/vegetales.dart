import 'package:flutter/material.dart';

class Vegetales extends StatelessWidget {
  const Vegetales({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Vegetales"),
      ),
    );
  }
}

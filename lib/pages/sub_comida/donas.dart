import 'package:flutter/material.dart';

class Donas extends StatelessWidget {
  const Donas({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Donas"),
      ),
    );
  }
}
